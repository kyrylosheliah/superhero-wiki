import { describe, it } from 'node:test';
import { findSuperheroById } from './SuperheroRepository';

describe('SuperheroRepository', () => {
  //it('should ...', async () => {
  //  const record = await findSuperheroById(123);
  //});
});
